{
  "name": "com.example.pizzazz",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "packageManager": "pnpm@10.28.0",
  "scripts": {
    "lint": "eslint --ext ts,tsx src",
    "test": "jest",
    "start": "stripe apps start",
    "build": "bash -c 'curl -fsSL https://packages.stripe.dev/stripe-cli/install.sh | bash && export PATH=\"$HOME/.stripe/bin:$PATH\" && stripe version && stripe apps build'"
  },
  "dependencies": {
    "@stripe/ui-extension-sdk": "9.2.0-alpha.12",
    "@tanstack/react-query": "5.101.2",
    "stripe": "^17.7.0"
  },
  "devDependencies": {
    "@stripe/ui-extension-tools": "0.0.1",
    "@types/react": "^17.0.93"
  }
}
